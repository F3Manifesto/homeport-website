import { FunctionComponent } from "react";

// get static props and paths here for the order info

const OrderInfo: FunctionComponent = (): JSX.Element => {
    return (
        <div className="relative w-full h-full grid grid-flow-row auto-rows-[auto auto]">
            <div className="relative w-40 h-40 bg-gray-400">

            </div>
        </div>
    )
}

export default OrderInfo;