import { NextPage } from "next";

const Success: NextPage = (): JSX.Element => {
  return (
    <div className="relative grid grid-flow-col auto-cols-[auto auto] w-full h-full min-h-screen justify-center gap-20 pt-20 text-white">
        Payment Success!! Explore other styles!
    </div>
  );
};

export default Success;
